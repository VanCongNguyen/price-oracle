# Copy this file to .env.local and fill in your own values.

# Optional. The product name shown in the header and browser tab.
NEXT_PUBLIC_APP_NAME=Price Oracle

# Backend URL the browser calls directly. Change this if the backend
# is deployed on a different host/port than the default.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
